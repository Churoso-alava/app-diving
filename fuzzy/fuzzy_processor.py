# from fuzzy.fuzzy_rules import crear_motor_v42  <-- Lo comentamos por ahora

def ejecutar_calculo(w_val, ci_val):
    """
    Modo temporal (Bypass) para no bloquear la interfaz.
    Devuelve un 50.0% estático hasta que construyamos las reglas reales.
    """
    return 50.0
    